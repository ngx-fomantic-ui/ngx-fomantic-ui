language: node_js
node_js:
  - "9"
dist: trusty
sudo: required
before_script:
  - npm install -g @angular/cli

jobs:
  include:
    # TODO: Fix ng lint
    #- stage: "Tests"
    #  name: "Angular lint"
    #  script:
    #    - ng lint
    - stage: "ngx-fomantic-ui library"
      script:
        - npm run build:lib:prod
    - stage: "ngx-fomantic-ui documentation"
      script:
        - npm run build:docs:prod
